---
name: step1_anc_partner_community_referral_date
description: Date ANC Partner community referral was submitted
priority: 1
condition: "true"
actions:
  - "def getDate(dateFormat) {
        import java.util.*;
        import java.text.SimpleDateFormat;
        return new SimpleDateFormat(dateFormat).format(System.currentTimeMillis());
    }"
  - "calculation = getDate('yyyy-MM-dd')"
